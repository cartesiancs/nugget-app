import React from "react";

/**
 * Floating button that opens the chat sidebar.
 * This component is now disabled as we use the chat toggle button in App.ts instead.
 */
export default function FloatingChatButton({ open, setOpen }) {
  // Return null to disable this component since we're using the chat toggle button in App.ts
  return null;
}
