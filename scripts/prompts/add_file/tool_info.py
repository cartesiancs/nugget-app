TOOL_INFO = {
    "add_file": {
        "description": "Adds a file to the timeline or canvas from the available files.",
        "params": {
            "fileName": {"type": "string", "description": "The name of the file to add. For example, if the user says 'add the file epic.mp4', this value should be 'epic.mp4'."}
        }
    }
} 